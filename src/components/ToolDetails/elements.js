import styled from 'styled-components';

export const ToolDetailsBar = styled.div`
    flex: 1;
    display: flex;
`;

export const Icon = styled.img`
    width: 1.125rem;
    filter: invert(100%);
    position: absolute;
    margin: auto;
    top: 0;
    left: 0;
    right: 0;
    bottom: 1px;
`;

export const ToolIcon = styled.div`
    width: 2.5rem;
    position: relative;
`;
